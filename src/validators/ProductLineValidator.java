package validators;

import contracts.ProductLine;

import java.math.BigDecimal;

public class ProductLineValidator {
//    public static void validateProductLine(String name, int quantity, BigDecimal unitPrice) {
//        if(name.trim().isBlank()|| quantity <0 || unitPrice.compareTo(new BigDecimal(quantity)) < 0) {
//            throw new IllegalArgumentException("Something wrong with ProductLine");
//        }
//    }
}
